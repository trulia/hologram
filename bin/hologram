#!/usr/bin/env ruby
require 'hologram'

begin
  if ARGV[0] == 'init'
    Hologram::DocBuilder.setup_dir
  else
    Hologram::DocBuilder.from_yaml(ARGV[0]).build
  end
rescue RuntimeError => e
  Hologram::DisplayMessage.error("#{e}")
end
